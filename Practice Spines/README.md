# Practice Spines

| File | Role |
|---|---|
| `Phase A MVP Spines.md` | Curated MVP lists for M1–M11 + coverage gaps (names, why, difficulty, LC, bank flag) |
| Per-module unlabeled pulls (optional later) | Timed blocks without pattern labels in the title |

**Primary spine:** use `Phase A MVP Spines.md` for what to drill. For timed sets, hide pattern names / bank tags from the student view.

Until unlabeled per-module files exist, pull from retention grills (hide keys) or `Gauntlet/Phase A Gauntlet.md`.

Local executable practice: `problem-bank/` (`py -3 run_all.py`).

Unlabeled timed mixes (no pattern titles): `Practice/Unlabeled Timed Sets/`.

## Gate rule

`python tools/spine_status.py --gate` must show the module **READY** before `timed-verified`.
LC-required rows (`Bank = —`) are real work — log them. Optional/exposure rows do not block.
