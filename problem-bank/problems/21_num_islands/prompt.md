# Number of Islands (LC 200)

Given an `m x n` 2D binary grid (lists of `"1"` land and `"0"` water), return the number of islands.
An island is surrounded by water and formed by connecting adjacent lands horizontally or vertically.

## API

```python
def num_islands(grid: list[list[str]]) -> int
```

You may mutate the grid.
