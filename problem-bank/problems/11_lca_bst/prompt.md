# Lowest Common Ancestor of a BST (LC 235)

Given a BST and two nodes `p` and `q` (by value), return the value of their lowest common ancestor.

All node values are unique. `p` and `q` exist in the tree.

## API

```python
def lowest_common_ancestor(root: TreeNode, p_val: int, q_val: int) -> int
```
