# Interval List Intersections (LC 986)
Closed intervals. Both lists pairwise disjoint and sorted by start.
## API
```python
def interval_intersection(first: list[list[int]], second: list[list[int]]) -> list[list[int]]
```
