# Binary Search (LC 704)

Given a sorted array of distinct integers `nums` and a `target`, return the index of `target`, or `-1` if not present.

## API

```python
def binary_search(nums: list[int], target: int) -> int
```
